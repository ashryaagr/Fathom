node_modules/
out/
dist/
*.tsbuildinfo
.DS_Store
*.log
.env
.env.local
.vite/
src/renderer/public/pdfjs-cmaps/
src/renderer/public/pdfjs-fonts/
src/renderer/public/pdfjs-wasm/

# Per-paper Fathom sidecar folders created next to PDFs.
*.pdf.fathom/
**/*.pdf.fathom/

# Local-only working notes and helper scripts. The actual git hooks
# live in .git/hooks/ (already untracked), but guard against any
# stray tracked hooks / local dirs too.
.local/
hooks/
.githooks/

# Generated app icons (built from resources/icon.svg by scripts/build-icon.mjs).
resources/icon.iconset/

# Pre-pivot whiteboard pipeline — kept locally for reference, not committed.
old/

# Build artifacts at repo root (Vite/esbuild output dropped in cwd).
/index.js
/index-*.js

# Local reference clone of excalidraw-mcp (the real vendor lives in
# fathom-whiteboard/vendor/, not here).
.vendor/

# Runtime lock file written by the agent harness.
.claude/scheduled_tasks.lock
